#include <iostream>
#include <string>

using namespace std;

template <class T>
class Queue
{
	
};

int main() {
	string type;
	int n;
	cin >> type >> n;
	for (int i = 0; i < n; i++) {	
		int number;
		string action;
		if (type == "int") {
			int a;	
			cin >> action >> number >> a;
		}
	}	
}
